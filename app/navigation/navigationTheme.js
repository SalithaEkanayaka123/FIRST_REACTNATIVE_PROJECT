import { DefaultTheme } from "@react-navigation/native";

export default {
    ...DefaultTheme,
    colors: {
        ...DefaultTheme.colors,
        primary: '#F3ED9E',
        background: '#FFFFFF'
    }
};